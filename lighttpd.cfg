server.document-root = var.CWD

server.port = 8060

mimetype.assign = (
        ".html" => "text/html", 
        ".txt" => "text/plain",
        ".mrl" => "text/plain",
        )

index-file.names = ( "index.html" )
